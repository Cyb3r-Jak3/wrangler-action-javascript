name: 'Action Test'
on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - '**'
    tags-ignore:
      - '**'
    paths:
      - "**.ts"
      - ".github/workflows/action_check.yml"
      - "action.yml"
      - "dist/index.js"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      # - name: Checkout
      #   uses: actions/checkout@v2

      - run: echo ${{ github.repository }}@${{ github.head_ref }}
      - name: Checkout Example Repo
        uses: actions/checkout@v2
        with:
          repository: Cyb3r-Jak3/worker-example-project
      
      - name: Test Action
        # uses: ${{ github.repository }}@${{ github.head_ref }}
        uses: Cyb3r-Jak3/wrangler-action-javascript@dev
        with:
          apiToken: ${{ secrets.TEST_TOKEN }}
          secrets: |
            HELLO
        env:
          HELLO: WORLD

